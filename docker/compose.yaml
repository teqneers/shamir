---
services:
  php:
    build:
      context: .
      args:
        PHP_VERSION: "${PHP_VERSION:-8.4}"
    restart: no
    env_file:
      - ./.env
    volumes:
      - ..:/web
    working_dir: /web
